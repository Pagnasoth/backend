# Example env file for backend
# If you have a Gemini-compatible endpoint, set these. If not, the server returns mocked analysis.
# IMPORTANT: Do NOT commit real API keys. Use a local `.env` (gitignored) with real values.
# To get a free API key: https://aistudio.google.com/apikey
# Steps:
# 1. Click "Get API Key" → "Create API Key in new project"
# 2. Copy your key and paste below
# 3. Choose a model from: gemini-2.0-flash (recommended), gemini-1.5-pro, gemini-1.5-flash
# 4. Save as server/.env and restart the backend
#
# Example (copy to server/.env and uncomment/fill in):
GEMINI_API_KEY=
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent
PORT=5174
